/* Declare uma variavel de nome weight
var weight 

//que tipo de dado é a variável acima?
console.log(typeof weight)

Declare uma variável e atribua valores para cada
    name: string
    age: number(int)
    stars: number (float)
    isSubscribed: boolean

let name = "Thay"
let age = 21 
let stars = 4.8
let isSubscribed = true
*/

/* 
- A variável student abaixo é de que tipo de dados? 
- Atribua a ela as mesmas propriedades e valores do ex anterior

let student= {
    name: "Thay" ,
    age = 21 ,
    stars = 4.8,
    isSubscribed = true
}

console.log(student)

- mostre no console a seguinte mensagem
< name > de idade <age> pesa <weight> kg.

>substitua <name> <age> e <weight> pelos valores de cada propriedade

let student= {
    name: "Thay" ,
    age = 21 ,
    weight = 4.8,
    isSubscribed = true
}

console.log(`${student.name} de idade ${student.age} pesa ${student.weight} kg`)


*Declare uma variável do tipo Array, de nome,
students e atribua a ela nenhum valor

var students = []

- reatribua valor ao Array com valores da questão anterior

let students= []

students = [
    student 
]

console.log(students)

- Coloque no console o valor da posição zero do array acima

console.log(students[0])

*Crie um novo student e coloque na posição 1 do array students

const carlos= {
    name: "Carlos" ,
    age = 32 ,
    weight = 70,
    isSubscribed = true
}

    students = [
        student,
        carlos
    ]
 
    console.log(students) ou students [1] = carlos

*/

